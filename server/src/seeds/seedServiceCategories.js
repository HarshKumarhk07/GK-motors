// The catalogue is seeded by the server itself on startup, and categories and
// packages are created together. This file is kept so the old command still
// works, and simply delegates.
require('./seedServicePackages');
